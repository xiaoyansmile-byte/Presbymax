# 暂未实现需求

这个文件用于记录已经确认、但当前会话里未完成的需求。  
和 `BUG_TRACKER.md` 不同，这里记录的是“还没做完、但需求本身已经明确”的事项，方便下次继续时直接按这里的说明恢复上下文。

## 记录格式

建议每条需求都写清楚：

- 目标模块
- 具体替换对象
- 约束条件
- 当前未完成原因
- 后续执行提示

---

## REQ-001 | OPTIcTRAIN 驾驶页素材替换

### 目标

把 `optictrain-visual-neuro-navigation-demo` 驾驶页里的 5 类场景素材替换成用户提供的 PNG 图片，同时保持原代码里的尺寸约束与绘制位置逻辑不变。

### 替换对象与素材映射

按下面顺序替换：

1. 钻石
   - 当前对象：`diamond`
   - 新素材：`/Users/hanxiaoyan/Downloads/icons8-diamond-100.png`
2. 石头
   - 当前对象：`rock`
   - 新素材：`/Users/hanxiaoyan/Downloads/icons8-rock-100.png`
3. 垃圾桶
   - 当前对象：`bin`
   - 新素材：`/Users/hanxiaoyan/Downloads/icons8-full-trash-100.png`
4. 交通锥桶
   - 当前对象：`cone`
   - 新素材：`/Users/hanxiaoyan/Downloads/icons8-vlc-100.png`
5. 小汽车
   - 当前对象：`car`
   - 新素材：`/Users/hanxiaoyan/Downloads/icons8-car-top-view-100.png`

### 额外约束

- 需要保持原代码中的尺寸约束，不要把素材直接拉伸到失真。
- 保持原有绘制位置、运动逻辑、碰撞逻辑和计分逻辑不变。
- 所有素材应尽量按原始比例显示。
- 小汽车素材的车头目前是朝右的，接入时需要逆时针旋转 90 度，让车头朝上。
- 这次替换只做素材接入，不要顺手改车道逻辑、HUD、规则弹窗、分数计算等其他模块。

### 当前状态

- 已完成。
- 5 类场景素材已替换为用户提供的 PNG 图片。
- 小汽车已按要求修正为车头朝上。
- 场景的随机出现、远近放大、层级遮挡与视觉反馈也已同步完善。

### 完成说明

- 已将钻石、石头、垃圾桶、交通锥桶和小汽车的素材接入 `Navigation` 驾驶页。
- 已保留原有尺寸约束、绘制位置逻辑、碰撞逻辑和计分逻辑。
- 已恢复并升级道路两侧树木与建筑素材。
- 已按要求修正车头方向，并完成相关视觉细节优化。

### 下次继续时的建议步骤

1. 如果后续还要调整视觉风格，可优先从远景密度、遮挡和反馈动效继续优化。
2. 如果页面运行方式发生变化，优先检查静态资源引用路径是否仍然正确。
3. 继续时尽量不要同时引入过多新的训练闭环改动，避免影响当前稳定版本。

### 备注

如果后面继续做这项，优先检查：

- PNG 是否带透明背景
- 素材是否仍按 400x400 的原始比例显示
- 车的朝向是否正确
- 替换后是否影响 Gabor 或驾驶场景的识别度

---

## REQ-002 | 隧道训练参数后台化

### 目标

把 `tunnel-symbol-speed` 迁移后的产品页参数，后续接入管理员训练配置，而不是继续依赖页面内 mock 常量。

### 需要后台化的参数

1. 时长
2. 隧道行进速度
3. 字符移动速度

### 当前状态

- 已在产品页里先用 mock 常量实现，方便先完成 Demo 迁移。
- 后续不再改数据结构的前提下，再把这三个参数逐步接到管理员训练配置。

### 当前不直接处理的原因

- 这三个参数涉及训练配置数据结构与管理页联动。
- 现在优先保证迁移完成和交互闭环稳定，避免把多处页面同时改乱。

### 后续执行提示

- 等管理员训练配置页准备好对应字段后，再把 mock 常量替换为真实配置读取。
- 迁移时要同步确认训练记录里是否需要保存这些参数快照，避免后续回放口径不一致。
